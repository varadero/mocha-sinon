export class FullCoverage {
    add(first: number, second: number): number {
        return first + second;
    }

    subtract(first: number, second: number): number {
        return first - second;
    }

    multiply(first: number, second: number): number {
        return first * second;
    }
}